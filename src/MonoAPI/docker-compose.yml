version: '3.4'

services:
  web-spa:
    image: keeper/webspa
    build:
      context: .
      dockerfile: web-spa/Dockerfile
  
  mono-api:
    image: keeper/monoapi
    build:
      context: .
      dockerfile: MonoAPI/Dockerfile
      
  mongodb:
    image: mongodb/mongodb-community-server:6.0-ubi8
